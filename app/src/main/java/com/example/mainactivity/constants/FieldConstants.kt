package com.example.mainactivity.constants


/* Данный класс перечислений создан для хранения постоянных значений
в нашем приложении,тоесть для хранения объявленных констант */
enum class FieldConstants (val value: Int) {
    /* Добавляем в класс перечислений константы для числа столбцов
    и строк игрового поля, а именно 10 столбцов поля  и 20 строк */
    COLUMN_COUNT(10),
    ROW_COUNT(20)
    //(КОЛОНКА(КАЛЭМ)_СЧЕТ) И (РЯД(РОУ)_СЧЕТ)
}